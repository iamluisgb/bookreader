name = "bookreader-auth"
main = "src/index.js"
compatibility_date = "2026-07-01"

# account_id no es necesario si has hecho `wrangler login` con una sola cuenta;
# si wrangler lo pide, sale de `npx wrangler whoami`.

[vars]
# Client ID público del OAuth client de Google (paso 4 de docs/GUIA_CLOUDFLARE.md)
GOOGLE_CLIENT_ID = "1075634570118-8ipsh5pn768rof3u0mq5rhqv2pp2dgqm.apps.googleusercontent.com"
# Orígenes permitidos por CORS, separados por comas (dev + producción)
ALLOWED_ORIGINS = "http://localhost:8000,https://luisgonzalezbernal.com"
